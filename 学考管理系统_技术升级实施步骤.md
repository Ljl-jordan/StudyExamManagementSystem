# 学考管理系统技术升级实施步骤

> 来源：`学考管理系统技术升级开发文档.docx`  
> 目标：在现有学考管理系统基础上接入 Redis 缓存层与 AI 助手，并形成可运行、可测试、可回滚、可量化的技术升级成果。  
> 范围：只做 Redis 缓存、Redisson 分布式锁、AI 对话与 RAG 问答，不做微服务拆分、集群高可用和模型训练。

## 一、升级前必须确认的决策

- [ ] **框架路线**：文档建议 Java 17 + Spring Boot 3.5.x + Spring AI 1.x；当前项目实际为 Java 8 + Spring Boot 2.1.2，必须先完成框架升级。
- [ ] **持久层路线**：当前项目实际使用 Spring Data JPA，不是文档中假设的 MyBatis-Plus。一期建议继续使用 JPA，不在本轮同时迁移持久层。
- [ ] **AI 模型路线**：默认接入 DeepSeek API；内网或数据不能出网时，使用 Ollama + Qwen 作为备选。
- [ ] **向量方案**：向量检索使用 Redis Stack；Embedding 使用 BGE-M3，并与对话模型解耦。
- [ ] **版本锁定**：文档中的版本为建议值，实施前必须根据 Spring Boot、Spring AI、Redisson 的兼容矩阵确认最终版本并锁定。
- [ ] **旧接口不变量**：现有接口路径、参数和返回结构保持不变，缓存与 AI 都作为旁路能力接入。

## 二、总体实施顺序

建议严格按以下顺序推进，每一阶段的验收门槛通过后再进入下一阶段：

1. 冻结基线并备份。
2. 完成 Java 与 Spring Boot 升级。
3. 搭建 Redis / Redisson / AI 基础环境。
4. 实现统一缓存服务层。
5. 按业务场景逐个接入缓存。
6. 完成缓存穿透、击穿、雪崩防护。
7. 落地分布式锁与幂等控制。
8. 实现 AI 模型适配层与基础对话。
9. 建设知识源入库与 RAG 检索链路。
10. 完成数据权限、考试保护和限流。
11. 完成前端联调。
12. 完成可观测性、压测与验收。
13. 灰度上线、验证回滚。
14. 整理实测数据，更新项目文档和简历描述。

## 三、分阶段执行步骤

### 阶段 0：冻结基线并备份

- [ ] 创建独立 Git 功能分支，例如 `feature/redis-ai-upgrade`。
- [ ] 备份数据库、关键配置和当前可运行版本。
- [ ] 确认 MySQL、Redis Stack、Ollama 和 DeepSeek API Key 的可用性。
- [ ] 跑通现有登录、权限、知识库、学习任务、题库、试卷、答题、导出和日志接口。
- [ ] 记录热点接口改造前的平均耗时、P95、数据库查询次数和缓存命中率基线。
- [ ] 记录数据库表结构、接口数量和关键业务流程，作为升级后的回归依据。

**退出条件：现有系统可正常启动，核心接口回归通过，已有可重复使用的性能基线。**

### 阶段 1：升级 Java 与 Spring Boot

- [ ] 升级 Java、Spring Boot、Gradle 和插件版本。
- [ ] 将 `javax.*` 迁移为 `jakarta.*`。
- [ ] 升级或替换 Springfox、JWT、POI、Lombok、MySQL 驱动等兼容性依赖。
- [ ] 调整 Spring Boot 3 的 YAML、JPA、拦截器和序列化配置。
- [ ] 修复编译错误、启动错误和运行时兼容问题。
- [ ] 完整回归现有接口和定时任务。
- [ ] 本阶段不接入 Redis 和 AI，避免多类问题同时出现。

**退出条件：项目使用新框架稳定启动，原有功能全部可用。**

### 阶段 2：搭建 Redis 与基础设施

- [ ] 启动 Redis Stack 7.x。
- [ ] 引入 Spring Data Redis 和 Redisson。
- [ ] 配置 Redis 地址、端口、密码、数据库、连接池和超时。
- [ ] 配置统一 JSON 序列化，避免使用不兼容的 JDK 序列化。
- [ ] 建立缓存 Key 命名规范。
- [ ] 增加 Redis 健康检查和缓存开关。
- [ ] 验证 Redis 停止后，业务请求能够降级直连数据库。

**退出条件：Redis 可连接、可读写，停止 Redis 不影响主流程启动和核心接口调用。**

### 阶段 3：实现统一缓存服务层

- [ ] 封装统一的缓存查询、写入、删除、空值缓存和异常降级逻辑。
- [ ] 实现随机 TTL，避免大量 Key 同时过期。
- [ ] 实现互斥重建，防止热点 Key 失效时并发击穿数据库。
- [ ] 实现更新数据库后删除缓存的 Cache Aside 模式。
- [ ] 实现延迟双删或等价的一致性补偿方案。
- [ ] 对敏感字段进行缓存前剔除，禁止将试题答案、解析等数据写入普通缓存。
- [ ] 提供缓存命中率、未命中次数和降级次数统计。

**退出条件：缓存服务层可独立测试，Redis 异常时自动降级，不改变业务返回结构。**

### 阶段 4：按业务场景接入缓存

建议按风险和收益逐个接入，不要一次性全量改造：

1. [ ] 知识素材详情：Cache Aside + 互斥锁，TTL 约 30 分钟并增加随机偏移。
2. [ ] 知识库分类树：按组织隔离，TTL 约 10 分钟并增加随机偏移。
3. [ ] 学习任务列表与进度：写操作后主动失效，TTL 约 10 分钟。
4. [ ] 试题详情：只缓存题干，空值缓存防穿透，TTL 约 60 分钟。
5. [ ] 已发布试卷列表：发布、取消发布、归档时主动删除缓存，TTL 约 10 分钟。

每个场景完成后必须验证：

- [ ] 首次请求查库并回填缓存。
- [ ] 第二次请求命中缓存。
- [ ] 更新数据后缓存被正确删除。
- [ ] 敏感字段未进入缓存。
- [ ] Redis 停止后仍能访问数据库。

**退出条件：至少一个低风险场景完整闭环，再逐个扩展其他场景。**

### 阶段 5：完成缓存三大问题防护

- [ ] 缓存穿透：增加空值缓存；按进度决定是否增加布隆过滤器。
- [ ] 缓存击穿：使用 Redisson 或 SETNX 互斥锁重建热点缓存。
- [ ] 缓存雪崩：使用随机过期时间、降级直连和可选的本地 Caffeine 缓存。
- [ ] 准备 50 并发热点重建测试，验证数据库只被访问一次。
- [ ] 准备不存在 ID 的连续查询测试，验证空值缓存或布隆过滤器生效。
- [ ] 准备 Redis 宕机测试，验证主流程不受影响。

**退出条件：三类缓存问题均有代码实现、测试用例和可展示结果。**

### 阶段 6：接入 Redisson 分布式锁

优先落地以下三个场景：

1. [ ] 题库批量导入：同一导入批次只能成功入库一次。
2. [ ] 试卷发布：同一试卷并发发布时只能成功一次。
3. [ ] 在线答题提交：同一考生同一试卷只能成功提交一次。

每个锁场景都必须包含：

- [ ] 明确锁 Key。
- [ ] 合理设置等待时间和自动释放时间。
- [ ] 获取锁后进行数据库二次幂等检查。
- [ ] 确保数据库更新处于正确事务边界。
- [ ] 只由当前持锁线程释放锁。
- [ ] 数据库唯一约束作为最终兜底。
- [ ] 并发测试无重复数据、无死锁、无错误释放。

**退出条件：三类并发场景均通过并发测试，且锁失效时数据库约束仍能保证正确性。**

### 阶段 7：实现 AI 基础能力

- [ ] 定义统一 `AiModelClient` 接口。
- [ ] 实现 DeepSeek 客户端。
- [ ] 实现 Ollama + Qwen 备选客户端。
- [ ] 通过配置切换模型，业务层不直接依赖厂商 SDK。
- [ ] 增加超时、重试、熔断、错误码和兜底文案。
- [ ] 实现普通对话接口。
- [ ] 实现 SSE 流式返回。
- [ ] 使用 Redis 或 Spring AI RedisChatMemory 保存会话历史。
- [ ] 会话窗口建议保留最近 10 轮，并设置自动过期。
- [ ] `/api/ai/**` 接入现有 JWT 和 `@RequirePermission` 权限体系。

**退出条件：DeepSeek 或 Ollama 任一模型可连续完成 10 轮对话，且模型切换不需要改业务代码。**

### 阶段 8：建设 RAG 知识源与入库链路

一期知识源：

- [ ] 知识库素材：支持富文本和附件。
- [ ] 题库：题干与题目解析，答案字段按权限决定是否入库。
- [ ] 二期再考虑试卷和学习任务。

入库步骤：

- [ ] 从 `kb_material` 读取富文本内容。
- [ ] 通过 `kb_material_file` 和 `sys_file` 解析 PDF、Word、Excel、TXT、Markdown。
- [ ] 按标题和段落切分，建议每块 500～800 字，并保留重叠。
- [ ] 使用 BGE-M3 生成向量。
- [ ] 将向量写入 Redis Stack。
- [ ] 为每个向量块保存 `sourceType`、`sourceId`、`materialId`、`questionId`、`orgId`、`dataScope` 和 `sensitive` 元数据。
- [ ] 提供全量重建和增量重建索引入口。

**退出条件：知识源能够稳定入库、重建和检索，并能定位到原始素材或题目。**

### 阶段 9：实现 RAG 问答链路

- [ ] 用户问题先做输入安全检查。
- [ ] 使用 BGE-M3 完成问题向量化。
- [ ] 按 `orgId` 和 `dataScope` 做检索前过滤。
- [ ] 默认召回 TopK 20。
- [ ] 检索后调用业务权限服务进行二次复核。
- [ ] 过滤考试敏感内容。
- [ ] 取前 5 条知识片段组装 Prompt。
- [ ] 调用 `AiModelClient` 生成回答。
- [ ] 返回引用来源和引用片段。
- [ ] 向量检索失败时降级为普通对话。

**退出条件：AI 能回答知识库和题库内问题，并给出可追溯引用。**

### 阶段 10：数据权限、考试保护与安全

- [ ] AI 接口统一经过 `TokenInterceptor`。
- [ ] AI 业务接口使用 `@RequirePermission`。
- [ ] 所有检索必须应用 `userId`、`orgId` 和 `dataScope`。
- [ ] 检索前使用元数据过滤，检索后再次通过 DAO 或权限服务复核。
- [ ] 进行中的考试涉及题目建立黑名单。
- [ ] 黑名单中的题目、答案和解析不得被检索或回答。
- [ ] 增加用户级限流，建议初始 10 次/分钟。
- [ ] DeepSeek API Key 使用环境变量或配置中心，禁止提交到仓库。
- [ ] 日志脱敏，不记录完整对话、答案和解析内容。
- [ ] 验证未登录、跨组织、考试中提问试题等场景全部被正确拦截。

**退出条件：权限用例全部通过，AI 无法绕过现有数据隔离和考试保护。**

### 阶段 11：前端与接口联调

- [ ] 按接口契约实现聊天窗口。
- [ ] 支持 SSE 流式输出。
- [ ] 支持会话历史回显和新建会话。
- [ ] 支持引用来源展示。
- [ ] 支持加载中、模型超时、限流和降级提示。
- [ ] 旧接口不增加旁路调用。
- [ ] 前后端统一错误码和会话失效处理。

**退出条件：用户可以完成提问、流式接收、查看引用和恢复历史会话的完整流程。**

### 阶段 12：可观测性与测试验收

至少记录以下指标：

- [ ] 缓存命中率、穿透率和锁等待耗时。
- [ ] 热点接口改造前后的平均耗时和 P95。
- [ ] AI 首字延迟、完整响应耗时和 Token 用量。
- [ ] RAG 召回条数和平均相似度。
- [ ] 权限过滤和考试保护拦截次数。
- [ ] Redis、DeepSeek、BGE-M3 的失败次数和降级次数。

核心测试场景：

- [ ] 素材详情首次请求比第二次请求明显更慢，第二次命中缓存。
- [ ] 50 并发重建同一热点 Key，数据库只查询一次。
- [ ] 不存在 ID 连续查询 100 次，数据库压力受控。
- [ ] 题库导入 1000 题、10 并发提交，只成功入库一次。
- [ ] 同一答卷重复提交 10 次，只成功一次。
- [ ] 连续对话 10 轮后仍能引用上文。
- [ ] 组织 A 用户无法检索组织 B 的素材。
- [ ] 考试中无法检索当前试卷答案和解析。
- [ ] 未登录访问 `/api/ai/**` 返回 401。
- [ ] 停掉 Redis 或 DeepSeek 后，主流程可按设计降级。

**退出条件：所有验收指标有实际测试数据，不直接使用文档中的建议值冒充结果。**

### 阶段 13：灰度上线与回滚

- [ ] 先上线依赖和配置。
- [ ] 灰度开启缓存功能。
- [ ] 上线分布式锁场景。
- [ ] 上线 AI 基础对话。
- [ ] 上线 RAG 检索。
- [ ] 上线数据权限和考试保护。
- [ ] 缓存和 AI 设置独立开关。
- [ ] 任一阶段异常时，只关闭对应能力，不影响原业务主流程。

建议配置开关：

- [ ] 缓存总开关：关闭后直连数据库。
- [ ] AI 总开关：关闭后 AI 接口返回明确的不可用状态。
- [ ] 模型提供方：支持 `deepseek` / `ollama` 配置切换。
- [ ] 用户限流开关。
- [ ] 考试敏感内容保护开关，默认开启。

### 阶段 14：整理最终交付物

- [ ] 数据库及依赖升级记录。
- [ ] Redis Key 规范文档。
- [ ] 缓存一致性说明。
- [ ] 分布式锁使用场景和边界说明。
- [ ] AI 模型适配层说明。
- [ ] RAG 入库和问答流程图。
- [ ] 权限与考试保护测试报告。
- [ ] 压测报告和真实性能数据。
- [ ] 上线及回滚操作清单。
- [ ] 更新后的项目 README。
- [ ] 使用真实数据更新简历项目描述。

## 四、关键技术约束

- 不允许把试题答案、解析和考试敏感内容写入普通缓存。
- 不允许仅依赖 Prompt 做数据权限，必须在代码层完成过滤。
- 不允许用缓存替代数据库唯一约束。
- 不允许使用 `KEYS *` 清理缓存，批量操作应使用 SCAN 或维护明确 Key。
- 不允许使用无界线程池执行延迟双删和异步任务。
- 不允许把 DeepSeek API Key、Redis 密码等敏感信息提交到 Git。
- 不允许在旧接口未回归通过前接入 AI 和缓存。
- 不允许把设计目标值写成已经实现的实测数据。

## 五、最终验收目标

- [ ] 原系统功能零回归。
- [ ] Redis 缓存链路可运行、可降级、可观测。
- [ ] 缓存穿透、击穿、雪崩均有测试用例。
- [ ] 题库导入、试卷发布、答题提交均具备幂等和并发保护。
- [ ] AI 支持多模型配置切换和流式对话。
- [ ] RAG 能回答知识库与题库内容并返回引用。
- [ ] AI 全程复用 JWT、接口权限和组织数据权限。
- [ ] 考试进行中无法通过 AI 获取当前试卷答案或解析。
- [ ] 缓存和 AI 均可通过配置开关独立回滚。
- [ ] 形成真实可复现的性能与功能验收报告。